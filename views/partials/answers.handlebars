<div class="answers">
  {{#each this}}
  <section class="answer">
    <div class="meta">
      {{user.username}} on {{format_date createdAt}}
    </div>
    <div class="answer-text-box"">
      {{answer_text}}
    </div>
  </section>
  {{/each}}
</div>