<article class="post">
      <div class="title">
        <h3>{{user.username}}|<span>updated on {{format_date updatedAt}}</span></h3>
        <p>{{question_title}}(<span>{{question_text}}</span>)</p>
      </div>
      <div class="meta">
        
      </div>
    </article>